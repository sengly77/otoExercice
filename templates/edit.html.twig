{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <h1>Modification de {{ personne.nom }} {{ personne.prenom }}</h1>
            {% include 'form.html.twig' %}

            <button class="btn waves-effect waves-light" type="submit" name="action" style="border: 1px solid;margin-top:2%;">Modifier</button>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}