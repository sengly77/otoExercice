{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <h1>Ajout de personne</h1>

        <div class="row">

            <div class="row">
            {% include 'form.html.twig' %}

            <button class="btn waves-effect waves-light" type="submit" name="action" style="border: 1px solid;margin-top:2%;">Ajouter</button>

            {{ form_end(form) }}
            </div>

        </div>

    </div>
{% endblock %}